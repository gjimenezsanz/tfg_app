name: app2
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ">=2.19.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  firebase_core: ^3.12.0
  http: ^1.4.0
  fl_chart: ^0.63.0

  # Uso de Hive para almacenamiento local
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  flutter_markdown: ^0.6.10 # convertir Markdown en widgets estilizados.
  url_launcher: ^6.1.7 # abrir enlaces web

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  firebase_core: ^3.12.0
  firebase_auth: ^5.5.0
  cloud_firestore: ^5.6.4
  #flutter_ai_toolkit: ^0.6.8
  #firebase_ml_model_downloader: ^0.3.3+1
  hive_generator: ^2.0.1
  build_runner: ^2.4.15

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/users/ # imagenes usuarios
    - assets/questions.json # Preguntas cuestionario
